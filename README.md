# kishanbatra.github.io

Personal site — *Above 14th Street*. A scrolling parallax subway ride from the Manhattan skyline down to the 14 St–Union Sq platform and uptown to 116 St.

Plain static site: `index.html` + `styles.css` + `main.js`. No frameworks, no build step, no rasters — all artwork is inline/procedural SVG.
